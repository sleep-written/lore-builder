import { RegexBlurDirective } from './regex-blur.directive';

describe('RegexBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new RegexBlurDirective();
    expect(directive).toBeTruthy();
  });
});
