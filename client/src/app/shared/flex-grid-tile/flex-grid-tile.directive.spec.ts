import { FlexGridTileDirective } from './flex-grid-tile.directive';

describe('FlexGridTileDirective', () => {
  it('should create an instance', () => {
    // const directive = new FlexGridTileDirective();
    // expect(directive).toBeTruthy();
  });
});
